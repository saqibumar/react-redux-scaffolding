// src/components/NotFound.js (Optional)
import React from 'react';
import logo from '../logo.svg';

const NotFound = () => {
  return (
    <div>
        <h1>Page Not Found</h1>
        <img src={logo} className="App-logo" alt="logo" />
    </div>
    );
};

export default NotFound;
